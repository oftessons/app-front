export interface MetricaTema {
    tema: string;
    totalAcertos: number;
    totalRespondidas: number;
}

export interface MetricaSubtema {
    tema: string;
    subtema: string;
    totalAcertos: number;
    totalRespondidas: number;
}